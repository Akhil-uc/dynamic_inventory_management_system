# Dynamic Inventory Management System

## Requirements

Python 3.8+ (standard library only — no third-party dependencies).

## Running the Application

```bash
python app.py
```

You'll get a menu to add, search, update, delete, and display products,
place and process customer orders, and view low-stock products.

## Running the Tests

```bash
pip install pytest
python -m pytest tests/ -v
```